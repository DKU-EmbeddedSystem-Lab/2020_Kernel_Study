#tokenize cani and graph

from matplotlib import pyplot as plt

def tokenize(fileno):
    for line in fileno:
        #line = line.strip()
        #if line is not 
        print(line)

    return 2



def graph_ize(graph):
    print(graph)





if __name__ == '__main__':
    #get file
    fno = open(cani, mode = 'rt')
    #token
    getgraph = tokenize(fno)
    close(fno)
    #show graph
    graph_ize(graph)
    #show()
